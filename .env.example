# Supabase — projeto site-system-ir (IR Core). Ver apps/sistema/.env.example
# e apps/site/.env.example para as variáveis por app; nada aqui é lido
# diretamente por nenhum app (cada workspace do Next.js só lê seu próprio
# .env.local). NUNCA commitar valores reais, token pessoal (sbp_...) ou
# SUPABASE_SERVICE_ROLE_KEY neste repositório.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
